server:
  port: 8081
spring:
  application:
  name: business-service
  datasource:
    url: "jdbc:mysql://localhost:3306/system_design?serverTimezone=Asia/Seoul"
    username: "root"
    password: "8384"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  data:
    redis:
      host: localhost
      port: 6379